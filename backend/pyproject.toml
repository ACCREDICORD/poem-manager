[project]
name = "poem-manager-backend"
version = "0.1.0"
description = "Poem management system backend (FastAPI + SQLite + DeepSeek)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy>=2.0",
    "pydantic>=2.7",
    "httpx>=0.27",
    "python-multipart>=0.0.9",
]

[tool.uv]
package = false
